<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ggluo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ggluo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-30T13:05:02+00:00</updated><id>https://ggluo.github.io/feed.xml</id><title type="html">blank</title><subtitle>A computational imaging scientist. </subtitle><entry><title type="html">ZeRO-1 Distributed Optimizer - A Deep Dive</title><link href="https://ggluo.github.io/blog/zero1-distributed-optimizer" rel="alternate" type="text/html" title="ZeRO-1 Distributed Optimizer - A Deep Dive"/><published>2026-04-30T10:00:00+00:00</published><updated>2026-04-30T10:00:00+00:00</updated><id>https://ggluo.github.io/blog/zero1</id><content type="html" xml:base="https://ggluo.github.io/blog/zero1-distributed-optimizer"><![CDATA[<h2 id="overview">Overview</h2> <p>This is a deep dive into <strong>ZeRO Stage 1</strong> — the Distributed Optimizer from the <a href="https://arxiv.org/abs/1910.02054">ZeRO paper</a>. It covers the fundamentals and a complete toy implementation (~300 lines of core code). The implementation discussed throughout is open-sourced at <a href="https://github.com/ggluo/zero-optim">zero_optim_toy</a>, built on the original work by <a href="https://github.com/xiabingquan/zero_optim">@xiabingquan</a>.</p> <p><strong>Prerequisite</strong>: familiarity with Distributed Data Parallel (DDP).</p> <p>The key idea: within a data-parallel group, all \(N\) ranks hold <strong>identical</strong> optimizer states. ZeRO-1 partitions the optimizer state across ranks so each rank holds only \(1/N\) of it — the fp32 master parameters, Adam’s first moment (\(m\)), and second moment (\(v\)). After a local optimizer step on each shard, ranks exchange results via all-gather to recover the full updated parameters.</p> <h3 id="memory-savings">Memory Savings</h3> <p>For a bf16 model with Adam optimizer and \(P\) parameter elements:</p> <table> <thead> <tr> <th>Memory Item</th> <th>Vanilla DDP (per rank)</th> <th>ZeRO-1 (per rank)</th> </tr> </thead> <tbody> <tr> <td>fp32 master params</td> <td>\(4P\) bytes</td> <td>\(4P / N\) bytes</td> </tr> <tr> <td>Adam \(m\) (first moment)</td> <td>\(4P\) bytes</td> <td>\(4P / N\) bytes</td> </tr> <tr> <td>Adam \(v\) (second moment)</td> <td>\(4P\) bytes</td> <td>\(4P / N\) bytes</td> </tr> <tr> <td><strong>Optimizer state subtotal</strong></td> <td><strong>\(12P\) bytes</strong></td> <td><strong>\(12P / N\) bytes</strong></td> </tr> </tbody> </table> <p>With \(N=8\), optimizer state drops from \(12P\) to \(1.5P\). Model parameters (bf16) and gradient buffers remain at full size on each rank.</p> <h3 id="communication-volume">Communication Volume</h3> <table> <thead> <tr> <th>Operation</th> <th>Vanilla DDP</th> <th>ZeRO-1</th> </tr> </thead> <tbody> <tr> <td>Gradient sync</td> <td>all-reduce (\(\sim 2P\))</td> <td>reduce-scatter (\(\sim P\))</td> </tr> <tr> <td>Parameter broadcast</td> <td>none</td> <td>all-gather (\(\sim P\))</td> </tr> <tr> <td><strong>Total</strong></td> <td><strong>\(\sim 2P\)</strong></td> <td><strong>\(\sim 2P\)</strong></td> </tr> </tbody> </table> <p>ZeRO-1 adds an all-gather round, but total communication volume stays the same — which is why it is effectively standard for large-scale training today.</p> <h3 id="applicability">Applicability</h3> <ul> <li>Works for any element-wise optimizer (Adam, SGD, Adagrad, etc.) since each rank can independently update its shard and produce the same result as a full update.</li> <li><strong>Not</strong> directly applicable to optimizers that require global parameter statistics (e.g., Muon, LAMB with global norms) without extra communication.</li> </ul> <h3 id="limitations">Limitations</h3> <ul> <li>Does <strong>not</strong> save parameter or gradient memory — that’s the job of ZeRO Stage 2 and 3.</li> <li>Adds one all-gather round per step, which introduces latency (can be hidden by overlapping with forward computation).</li> </ul> <h2 id="how-it-works">How It Works</h2> <h3 id="partitioning-rationale">Partitioning Rationale</h3> <p>Within a data-parallel (or combined dp-cp) group, all ranks have identical model parameters, and therefore identical optimizer states. Instead of storing \(N\) redundant copies, we keep one copy split into \(N\) shards, one per rank.</p> <h3 id="communication-pattern">Communication Pattern</h3> <p>Vanilla DDP — each rank needs the <strong>full</strong> reduced gradient to update the <strong>full</strong> parameters:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>backward → gradient all-reduce → optimizer.step()
</code></pre></div></div> <p>ZeRO-1 — each rank only needs \(1/N\) of the reduced gradient for its own shard:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>backward → gradient reduce-scatter → optimizer.step(shard) → parameter all-gather
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">reduce-scatter</code> = reduce + scatter in a single collective. Each rank receives only the segment of the reduced gradient that it needs for its optimizer shard.</p> <h3 id="process-groups">Process Groups</h3> <p>From the optimizer’s perspective, fixing (tp_rank, pp_rank), the ranks across (dp_rank, cp_rank) form a group with identical optimizer states. The communication group for ZeRO-1 is therefore the dp-cp group (or a subset thereof).</p> <ul> <li><strong>Simple case</strong> (<code class="language-plaintext highlighter-rouge">num_optimizer_instances=1</code>): the entire dp-cp group is one optimizer’s partitioning domain.</li> <li><strong>Redundant case</strong> (<code class="language-plaintext highlighter-rouge">num_optimizer_instances&gt;1</code>): the dp-cp group is further split into: <ul> <li><strong>intra group</strong>: ranks within a single optimizer instance, used for reduce-scatter and all-gather.</li> <li><strong>inter group</strong>: corresponding ranks across different optimizer instances, used for cross-instance gradient reduce.</li> </ul> </li> </ul> <p>With redundancy, gradient sync becomes a two-phase process: reduce-scatter within intra group, then all-reduce across inter group. Parameter sync (all-gather) only happens within the intra group.</p> <h2 id="core-implementation">Core Implementation</h2> <p>The implementation follows a clean three-layer architecture: <strong>Buffer → DDP → Distributed Optimizer</strong>. Each layer has a distinct responsibility and communicates through clear interfaces.</p> <h3 id="buffer-layer">Buffer Layer</h3> <p><strong>Responsibility</strong>: manage contiguous memory, provide shard-level read/write and communication primitives.</p> <p>Why a buffer is necessary: model parameters and gradients are scattered across separate allocations in memory. Communicating per-tensor means one NCCL call per parameter — the kernel launch overhead becomes prohibitive. By remapping parameters and gradients into a contiguous buffer, a single collective call covers the entire buffer.</p> <p>The buffer manages the low-precision (e.g., bf16) parameters and gradients used in forward/backward — <strong>not</strong> the fp32 master parameters inside the optimizer (those are handled separately by the Distributed Optimizer layer).</p> <p>Key functions:</p> <ol> <li>Accept a list of parameters (with their dtypes) from the DDP layer and allocate corresponding contiguous buffers.</li> <li>Gradient buffer: always created (gradient communication is fundamental to DDP). Parameter buffer: only created when ZeRO-1 is enabled (vanilla DDP updates full parameters locally, no parameter communication needed).</li> <li>Remap <code class="language-plaintext highlighter-rouge">param.data</code> and <code class="language-plaintext highlighter-rouge">param.main_grad</code> to views into the buffer.</li> <li>Provide communication primitives: <code class="language-plaintext highlighter-rouge">reduce_scatter_grads()</code>, <code class="language-plaintext highlighter-rouge">allgather_params()</code>, <code class="language-plaintext highlighter-rouge">get_grad_shard(param)</code>, <code class="language-plaintext highlighter-rouge">write_param_shard(param, data)</code>.</li> </ol> <p><strong>Sharding and parameter boundaries</strong>: the buffer is evenly partitioned across \(N\) ranks along its contiguous memory — partition boundaries do <strong>not</strong> respect parameter boundaries. A single parameter may be split across two ranks. The upside: each shard has exactly equal size, making reduce-scatter / all-gather direct operations on uniform slices without padding. The tradeoff: the optimizer must handle partial parameter fragments.</p> <h3 id="ddp-layer">DDP Layer</h3> <p><strong>Responsibility</strong>: group parameters by type and precision, create buffers for each group, and provide unified gradient/parameter sync interfaces.</p> <p>Typical grouping example:</p> <table> <thead> <tr> <th>Param Type</th> <th>Precision</th> <th>Example</th> </tr> </thead> <tbody> <tr> <td>dense</td> <td>bf16</td> <td>Attention/FFN linear weights</td> </tr> <tr> <td>MoE</td> <td>bf16</td> <td>Expert FFN linear weights</td> </tr> <tr> <td>MoE</td> <td>fp32</td> <td>Router weights</td> </tr> </tbody> </table> <p>The DDP layer provides:</p> <ul> <li>Gradient sync interface (reduce-scatter or all-reduce, depending on whether ZeRO-1 is enabled).</li> <li>Parameter sync interface (all-gather, ZeRO-1 only).</li> <li>Gradient shard read and parameter shard write (forwarded to the Buffer layer).</li> </ul> <h3 id="distributed-optimizer-layer">Distributed Optimizer Layer</h3> <p><strong>Responsibility</strong>: manage optimizer state creation and update, coordinate precision conversion between low-precision (bf16) and high-precision (fp32) representations.</p> <p>This layer:</p> <ol> <li>Creates fp32 master copies and optimizer states <strong>only for the shard</strong> owned by the current rank (\(1/N\) the size).</li> <li>Wraps a standard Adam optimizer over the fp32 shard.</li> <li>Each training step executes: <ul> <li><strong>Gradient upcast</strong>: low-precision grad shard → <code class="language-plaintext highlighter-rouge">shard_main_param.grad</code> (fp32)</li> <li><strong>Parameter update</strong>: standard <code class="language-plaintext highlighter-rouge">Adam.step()</code> on the fp32 shard</li> <li><strong>Parameter writeback</strong>: updated fp32 shard → low-precision param buffer shard</li> <li><strong>Parameter sync</strong>: trigger all-gather to restore full parameters</li> </ul> </li> </ol> <h3 id="execution-flow">Execution Flow</h3> <p><strong>Initialization</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. DDP creates buffers
   ├─ Groups params by (type, precision), calls Buffer layer
   ├─ Remaps param.data and param.main_grad to buffer views
   └─ Computes shard range for each rank

2. Distributed Optimizer initialization
   ├─ Queries DDP for each parameter's shard info on this rank
   ├─ Creates fp32 master copy: shard_main_param = buffer_view[start:end].clone().float()
   └─ Creates Adam optimizer over the fp32 shard (Adam is unaware of distribution)
</code></pre></div></div> <p><strong>Training Step</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Forward
  └─ Uses full bf16 parameters in param buffer

Backward
  └─ autograd writes gradients to param.main_grad (views into grad buffer)

Gradient Sync (DDP)
  └─ reduce_scatter_grads(): each rank gets 1/N reduced gradient shard

Optimizer Step (Distributed Optimizer)
  ├─ 1. Upcast: shard_main_param.grad = grad_shard.float()
  ├─ 2. Adam.step() on the fp32 shard (local, 1/N size)
  ├─ 3. Writeback: write_param_shard(param, shard_main_param.to(bf16))
  └─ 4. Sync: allgather_params() → full parameters restored

Next Forward
  └─ Uses fully updated parameters
</code></pre></div></div> <h3 id="data-precision-pipeline">Data Precision Pipeline</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>grad buffer (bf16, full)
        │
   reduce-scatter
        │
   grad shard (bf16, 1/N)
        │
   .float()
        │
shard_main_param.grad (fp32, 1/N)
        │
   Adam.step()
        │
shard_main_param (fp32, 1/N)
        │
   .to(bf16)
        │
param buffer shard (bf16, 1/N)
        │
   all-gather
        │
param buffer (bf16, full)
</code></pre></div></div> <h2 id="optional-optimizations">Optional Optimizations</h2> <h3 id="communication-computation-overlap">Communication-Computation Overlap</h3> <ul> <li><strong>Gradient reduce-scatter with backward overlap</strong>: partition parameters into buckets. As soon as a bucket’s gradients are ready (detected via backward post-hooks), launch reduce-scatter without waiting for the full backward pass.</li> <li><strong>Parameter all-gather with forward overlap</strong>: after <code class="language-plaintext highlighter-rouge">optimizer.step()</code>, asynchronously launch all-gather for the first bucket. Before each layer’s forward, a pre-hook waits on that layer’s all-gather and dispatches the next bucket, forming a pipeline.</li> </ul> <h3 id="bucketing">Bucketing</h3> <p>Further subdividing buffers into buckets gives finer-grained overlap and reduces per-collective latency. Each bucket communicates independently.</p> <h3 id="buffer-padding--alignment">Buffer Padding &amp; Alignment</h3> <ul> <li>Pad to a multiple of <code class="language-plaintext highlighter-rouge">dp_world_size</code> (required for even partitioning in reduce-scatter).</li> <li>Align to 128-byte boundaries (cuBLAS/NCCL performance).</li> <li>Align to \(2^{16}\) for optimal NCCL ring algorithm bandwidth at large DP scales.</li> </ul> <h3 id="checkpoint-save--load">Checkpoint Save &amp; Load</h3> <p>The challenge: optimizer state is distributed across ranks. Common strategies:</p> <ul> <li><strong>Gather to rank 0</strong>: gather all shards to DP rank 0 and save a complete state.</li> <li><strong>Fully sharded save</strong>: each rank saves its own shard directly, enabling parallel I/O.</li> </ul> <h3 id="fp8-quantization-awareness">FP8 Quantization Awareness</h3> <p>When some parameters use FP8 precision for forward/backward, extra quantize/dequantize steps are needed: <code class="language-plaintext highlighter-rouge">fp8 → fp32 → optimizer → fp32 → fp8</code>. FP8 and non-FP8 parameters must live in separate buffers.</p> <h2 id="redundant-optimizers">Redundant Optimizers</h2> <p>In the redundant case, \(K\) optimizer replicas are kept within the dp-cp group. Each replica covers a subset (intra group) of size \(N/K\).</p> <p>For dp-cp group size \(N\) and total parameters \(P\):</p> <table> <thead> <tr> <th>Operation</th> <th>\(K=1\) (no redundancy)</th> <th>\(K&gt;1\) (with redundancy)</th> </tr> </thead> <tbody> <tr> <td>Gradient reduction</td> <td>reduce-scatter, \(\sim P\)</td> <td>reduce-scatter (intra) + all-reduce (inter), \(\sim P + \frac{2P}{N} \times (K-1)\)</td> </tr> <tr> <td>Parameter sync</td> <td>all-gather, \(\sim P\)</td> <td>all-gather (intra), \(\sim P \times \frac{N/K-1}{N/K}\)</td> </tr> <tr> <td><strong>Total</strong></td> <td><strong>\(\sim 2P\)</strong></td> <td><strong>\(&gt; 2P\)</strong></td> </tr> </tbody> </table> <p>Total communication <strong>strictly increases</strong> with redundancy on uniform-bandwidth topologies. However, the pattern changes — which can be advantageous under <strong>heterogeneous bandwidth</strong>.</p> <p>Example: \(N=8\), dp=4 (cross-node, slow link), cp=2 (intra-node, NVLink), \(K=4\):</p> <table> <thead> <tr> <th>Operation</th> <th>\(K=1\) slow-link data</th> <th>\(K=4\) slow-link data</th> </tr> </thead> <tbody> <tr> <td>Gradient reduction</td> <td>\(\sim P\)</td> <td>\(\sim 6P/8\) (only inter all-reduce on slow link)</td> </tr> <tr> <td>Parameter sync</td> <td>\(\sim P\)</td> <td>\(0\) (intra all-gather entirely on NVLink)</td> </tr> <tr> <td><strong>Slow-link total</strong></td> <td><strong>\(\sim 2P\)</strong></td> <td><strong>\(\sim 6P/8\)</strong></td> </tr> </tbody> </table> <p>Slow-link traffic drops by ~62.5%. The derivation for \(6P/8\): inter all-reduce with \(K=4\) ranks, each holding \(P/(N/K) = P/2\) data after intra reduce-scatter. Ring all-reduce cost: \(2 \times (K-1)/K \times P/2 = 2 \times 3/4 \times P/2 = 3P/4 = 6P/8\).</p> <p><strong>Caveat</strong>: NCCL already applies hierarchical algorithms for large cross-node communicators, so even with \(K=1\) the actual behavior may approximate a two-phase pattern. The marginal benefit of manual partitioning (\(K&gt;1\)) depends on whether NCCL’s automatic optimization is sufficient. The implementation complexity is non-trivial — two process groups, two-phase gradient sync with synchronization dependencies between them.</p> <h2 id="code-walkthrough">Code Walkthrough</h2> <p>The complete implementation is at <a href="https://github.com/ggluo/zero_optim_toy">zero_optim_toy</a>. The codebase also includes a <code class="language-plaintext highlighter-rouge">utils.py</code> module with shared distributed testing helpers, a package <code class="language-plaintext highlighter-rouge">__init__.py</code>, and a CLI via <code class="language-plaintext highlighter-rouge">__main__.py</code> (<code class="language-plaintext highlighter-rouge">python -m zero_optim_toy test</code>). Here we walk through each layer.</p> <h3 id="step-1-model">Step 1: Model</h3> <p>A simple test model — stacked MLP blocks with residual connections:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MLP</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">:</span> <span class="nb">int</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="n">hidden_dim</span> <span class="o">*</span> <span class="mi">4</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">act</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">GELU</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc2</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">act</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">fc1</span><span class="p">(</span><span class="n">x</span><span class="p">)))</span>

<span class="k">class</span> <span class="nc">ToyModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="o">=</span><span class="mi">512</span><span class="p">,</span> <span class="n">num_layers</span><span class="o">=</span><span class="mi">3</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">layers</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ModuleList</span><span class="p">([</span><span class="nc">MLP</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_layers</span><span class="p">)])</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="nf">layer</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <h3 id="step-2-buffer-layer">Step 2: Buffer Layer</h3> <p>The Buffer takes a list of tensors, flattens them into contiguous memory, and exposes shard views:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Buffer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">tensors</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">world_size</span><span class="p">,</span> <span class="n">dtype</span><span class="p">,</span> <span class="n">device</span><span class="p">):</span>
        <span class="n">numels</span> <span class="o">=</span> <span class="p">[</span><span class="n">t</span><span class="p">.</span><span class="nf">numel</span><span class="p">()</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">tensors</span><span class="p">]</span>
        <span class="n">total_numel</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">numels</span><span class="p">)</span>

        <span class="c1"># Pad to multiple of world_size for even reduce-scatter partitioning
</span>        <span class="n">self</span><span class="p">.</span><span class="n">padded_numel</span> <span class="o">=</span> <span class="n">math</span><span class="p">.</span><span class="nf">ceil</span><span class="p">(</span><span class="n">total_numel</span> <span class="o">/</span> <span class="n">world_size</span><span class="p">)</span> <span class="o">*</span> <span class="n">world_size</span>
        <span class="n">self</span><span class="p">.</span><span class="n">shard_numel</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">padded_numel</span> <span class="o">//</span> <span class="n">world_size</span>

        <span class="n">self</span><span class="p">.</span><span class="n">_data</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">padded_numel</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">dtype</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>

        <span class="c1"># _local_shard is a view — no extra memory allocation
</span>        <span class="n">shard_start</span> <span class="o">=</span> <span class="n">rank</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">shard_numel</span>
        <span class="n">shard_end</span> <span class="o">=</span> <span class="n">shard_start</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">shard_numel</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_local_shard</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">_data</span><span class="p">[</span><span class="n">shard_start</span><span class="p">:</span><span class="n">shard_end</span><span class="p">]</span>

        <span class="c1"># Create views into _data for each original tensor
</span>        <span class="n">self</span><span class="p">.</span><span class="n">_views</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">offset</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">for</span> <span class="n">t</span><span class="p">,</span> <span class="n">numel</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">tensors</span><span class="p">,</span> <span class="n">numels</span><span class="p">):</span>
            <span class="n">view</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">_data</span><span class="p">[</span><span class="n">offset</span> <span class="p">:</span> <span class="n">offset</span> <span class="o">+</span> <span class="n">numel</span><span class="p">].</span><span class="nf">view</span><span class="p">(</span><span class="n">t</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">_views</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">view</span><span class="p">)</span>
            <span class="n">offset</span> <span class="o">+=</span> <span class="n">numel</span>
</code></pre></div></div> <p>Communication primitives — symmetric and minimal:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">reduce_scatter</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">group</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="c1"># full buffer → local shard (SUM reduction)
</span>    <span class="n">dist</span><span class="p">.</span><span class="nf">reduce_scatter_tensor</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_local_shard</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">_data</span><span class="p">,</span> <span class="n">group</span><span class="o">=</span><span class="n">group</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">all_gather</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">group</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="c1"># local shard → full buffer
</span>    <span class="n">dist</span><span class="p">.</span><span class="nf">all_gather_into_tensor</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_data</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">_local_shard</span><span class="p">,</span> <span class="n">group</span><span class="o">=</span><span class="n">group</span><span class="p">)</span>
</code></pre></div></div> <p>Since <code class="language-plaintext highlighter-rouge">_local_shard</code> is a view into <code class="language-plaintext highlighter-rouge">_data</code>, writing to the shard writes directly into the correct region of the full buffer.</p> <h3 id="step-3-ddp-layer">Step 3: DDP Layer</h3> <p>The DDP layer creates two Buffers — one for parameters, one for gradients — and remaps <code class="language-plaintext highlighter-rouge">param.data</code> and <code class="language-plaintext highlighter-rouge">param.main_grad</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DistributedDataParallel</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">module</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">world_size</span><span class="p">,</span> <span class="n">process_group</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">params</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">module</span><span class="p">.</span><span class="nf">parameters</span><span class="p">())</span>
        <span class="n">self</span><span class="p">.</span><span class="n">param_buffer</span> <span class="o">=</span> <span class="nc">Buffer</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">world_size</span><span class="p">,</span>
                                   <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">bfloat16</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">grad_buffer</span> <span class="o">=</span> <span class="nc">Buffer</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">world_size</span><span class="p">,</span>
                                  <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">bfloat16</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_init_buffers</span><span class="p">()</span>

    <span class="k">def</span> <span class="nf">_init_buffers</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">param_views</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">param_buffer</span><span class="p">.</span><span class="nf">get_views</span><span class="p">()</span>
        <span class="n">grad_views</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">grad_buffer</span><span class="p">.</span><span class="nf">get_views</span><span class="p">()</span>
        <span class="k">for</span> <span class="n">param</span><span class="p">,</span> <span class="n">p_view</span><span class="p">,</span> <span class="n">g_view</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">,</span> <span class="n">param_views</span><span class="p">,</span> <span class="n">grad_views</span><span class="p">):</span>
            <span class="n">p_view</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">param</span><span class="p">.</span><span class="n">data</span><span class="p">)</span>
            <span class="n">param</span><span class="p">.</span><span class="n">data</span> <span class="o">=</span> <span class="n">p_view</span>         <span class="c1"># param.data now points into buffer
</span>            <span class="n">param</span><span class="p">.</span><span class="n">main_grad</span> <span class="o">=</span> <span class="n">g_view</span>    <span class="c1"># attach gradient view
</span></code></pre></div></div> <p>Gradient sync: the critical handoff from autograd to the buffer system. PyTorch’s autograd writes to <code class="language-plaintext highlighter-rouge">param.grad</code> (a separate tensor), so we must manually copy into the grad buffer view:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sync_grads</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">param</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">param</span><span class="p">.</span><span class="n">grad</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">param</span><span class="p">.</span><span class="n">main_grad</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">param</span><span class="p">.</span><span class="n">grad</span><span class="p">)</span>
            <span class="n">param</span><span class="p">.</span><span class="n">grad</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="n">self</span><span class="p">.</span><span class="n">grad_buffer</span><span class="p">.</span><span class="nf">reduce_scatter</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">process_group</span><span class="p">)</span>
</code></pre></div></div> <p>Parameter sync is a single all-gather:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sync_params</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">param_buffer</span><span class="p">.</span><span class="nf">all_gather</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">process_group</span><span class="p">)</span>
</code></pre></div></div> <h3 id="step-4-distributed-optimizer">Step 4: Distributed Optimizer</h3> <p>The simplest layer — clone the rank’s parameter shard as an fp32 master, hand it to standard Adam:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DistributedOptimizer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">ddp</span><span class="p">,</span> <span class="n">lr</span><span class="o">=</span><span class="mf">1e-3</span><span class="p">,</span> <span class="n">betas</span><span class="o">=</span><span class="p">(</span><span class="mf">0.9</span><span class="p">,</span> <span class="mf">0.999</span><span class="p">),</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-8</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">ddp</span> <span class="o">=</span> <span class="n">ddp</span>
        <span class="n">self</span><span class="p">.</span><span class="n">shard_fp32</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">Parameter</span><span class="p">(</span>
            <span class="n">ddp</span><span class="p">.</span><span class="nf">get_param_shard</span><span class="p">().</span><span class="nf">clone</span><span class="p">().</span><span class="nf">float</span><span class="p">(),</span>
            <span class="n">requires_grad</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">adam</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">optim</span><span class="p">.</span><span class="nc">Adam</span><span class="p">([</span><span class="n">self</span><span class="p">.</span><span class="n">shard_fp32</span><span class="p">],</span> <span class="n">lr</span><span class="o">=</span><span class="n">lr</span><span class="p">,</span> <span class="n">betas</span><span class="o">=</span><span class="n">betas</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="n">eps</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="c1"># 1. reduced grad shard (bf16) → fp32
</span>        <span class="n">self</span><span class="p">.</span><span class="n">shard_fp32</span><span class="p">.</span><span class="n">grad</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">ddp</span><span class="p">.</span><span class="nf">get_grad_shard</span><span class="p">().</span><span class="nf">float</span><span class="p">()</span>
        <span class="c1"># 2. Adam updates fp32 master
</span>        <span class="n">self</span><span class="p">.</span><span class="n">adam</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
        <span class="c1"># 3. fp32 → bf16, write back to param buffer shard
</span>        <span class="n">self</span><span class="p">.</span><span class="n">ddp</span><span class="p">.</span><span class="nf">write_param_shard</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">shard_fp32</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">bfloat16</span><span class="p">())</span>
        <span class="c1"># 4. all-gather to restore full parameters
</span>        <span class="n">self</span><span class="p">.</span><span class="n">ddp</span><span class="p">.</span><span class="nf">sync_params</span><span class="p">()</span>
</code></pre></div></div> <p>Adam sees only one fp32 tensor. The distribution is entirely transparent to the optimizer.</p> <h3 id="step-5-testing">Step 5: Testing</h3> <p>The test strategy: run the exact same precision path (bf16 forward/backward → fp32 Adam → bf16 writeback) on a single process without ZeRO-1, and compare parameters step-by-step against the multi-process ZeRO-1 run.</p> <p>All ranks use the same random seed for input data, and the loss is divided by <code class="language-plaintext highlighter-rouge">world_size</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">loss</span> <span class="o">=</span> <span class="n">output</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">world_size</span>
<span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>

<span class="n">ddp</span><span class="p">.</span><span class="nf">sync_grads</span><span class="p">()</span>
<span class="n">optimizer</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
</code></pre></div></div> <p>Since all ranks have identical gradients, after <code class="language-plaintext highlighter-rouge">reduce-scatter(SUM)</code>, each shard equals <code class="language-plaintext highlighter-rouge">N * (grad / N) = grad</code>, matching the reference exactly.</p> <p>Tests cover three scenarios:</p> <ul> <li>2-GPU multi-step training</li> <li>4-GPU multi-step training</li> <li><code class="language-plaintext highlighter-rouge">hidden_dim=13</code> (parameter count not divisible by <code class="language-plaintext highlighter-rouge">world_size</code>) — padding test</li> </ul> <p>The verification criterion is <strong><code class="language-plaintext highlighter-rouge">atol=0, rtol=0</code></strong> — bit-exact equality. Since the precision path is identical, ZeRO-1 results should match single-GPU training exactly.</p> <h3 id="profile-results">Profile Results</h3> <p>Model: 10-layer MLP with \(H=4096\), ~1.34B parameters, world_size=4.</p> <table> <thead> <tr> <th> </th> <th>Baseline (1 GPU)</th> <th>ZeRO-1 (4 GPUs)</th> </tr> </thead> <tbody> <tr> <td>bf16 model params</td> <td>2.50 GB</td> <td>2.50 GB</td> </tr> <tr> <td>grad_buffer</td> <td>—</td> <td>2.50 GB</td> </tr> <tr> <td>fp32 master params</td> <td>5.00 GB</td> <td>1.25 GB</td> </tr> <tr> <td>Adam \(m\)</td> <td>5.00 GB</td> <td>1.25 GB</td> </tr> <tr> <td>Adam \(v\)</td> <td>5.00 GB</td> <td>1.25 GB</td> </tr> <tr> <td><strong>Total</strong></td> <td><strong>17.50 GB</strong></td> <td><strong>8.75 GB</strong></td> </tr> </tbody> </table> <p>Theoretical saving: 50%. Optimizer state drops from 15.00 GB to 3.75 GB — a 75% reduction (i.e., \(1 - 1/N\)). However, ZeRO-1 introduces a persistent <code class="language-plaintext highlighter-rouge">grad_buffer</code> (2.50 GB) that baseline doesn’t have (baseline creates <code class="language-plaintext highlighter-rouge">param.grad</code> temporarily during backward and frees it during <code class="language-plaintext highlighter-rouge">optimizer.step()</code>). Net saving is ~42.7%, consistent with actual profile measurements.</p> <h2 id="summary">Summary</h2> <p>ZeRO-1’s core insight is simple: since data-parallel ranks hold identical optimizer states, keep one copy and shard it. The three-layer architecture (Buffer → DDP → Distributed Optimizer) cleanly separates distributed communication from optimization logic. With no increase in total communication volume, optimizer state memory drops to \(1/N\).</p> <p>For even larger models, ZeRO Stages 2 and 3 extend this idea further — sharding gradients and parameters as well — which would be the natural next topic to explore.</p>]]></content><author><name>Guanxiong Luo</name></author><summary type="html"><![CDATA[Overview]]></summary></entry><entry><title type="html">Let agent play with agent in shell script</title><link href="https://ggluo.github.io/blog/agent-shell" rel="alternate" type="text/html" title="Let agent play with agent in shell script"/><published>2026-04-28T21:49:00+00:00</published><updated>2026-04-28T21:49:00+00:00</updated><id>https://ggluo.github.io/blog/new</id><content type="html" xml:base="https://ggluo.github.io/blog/agent-shell"><![CDATA[<h3 id="motivation">Motivation</h3> <p>Deepseek V4 was released few days ago. I wanted to test the capability of Deepseek to prepare lecture slides and course materials on the Conjugate Gradient algorithm for MRI reconstruction. Rather than iterating manually, I wondered: can I have one agent create the materials and another agent review them, then loop this automatically? The answer turned out to be a ~400-line shell script.</p> <p>The idea is simple: wrap the <code class="language-plaintext highlighter-rouge">claude</code> CLI in a bash <code class="language-plaintext highlighter-rouge">for</code> loop, assign different roles (Student and Teacher) to each invocation, and let them coordinate through files on disk. Three rounds of back-and-forth produced a 26-page LaTeX Beamer lecture and a working multi-coil MRI reconstruction demo. Here is how it works.</p> <h3 id="architecture">Architecture</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for round in 1..3:
    ┌────────────┐             ┌────────────┐
    │  STUDENT   │ ─produces─▶ │  TEACHER   │
    │  (agent)   │             │  (agent)   │
    └────────────┘             └────────────┘
         ▲                           │
         │     feedback file         │
         └───────────────────────────┘
</code></pre></div></div> <p>Each invocation of <code class="language-plaintext highlighter-rouge">claude -p</code> runs as a separate process with no shared memory. The only communication channel is the filesystem: the Student writes slides and code, the Teacher reads them and writes a review to <code class="language-plaintext highlighter-rouge">feedback/round-N.md</code>, and the Student reads that review in the next round.</p> <h3 id="the-shell-script-at-a-glance">The shell script at a glance</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">ROUNDS</span><span class="o">=</span>3
<span class="k">for </span>round <span class="k">in</span> <span class="si">$(</span><span class="nb">seq </span>1 <span class="nv">$ROUNDS</span><span class="si">)</span><span class="p">;</span> <span class="k">do</span>
    <span class="c"># Phase 1: Student creates or improves materials</span>
    <span class="o">(</span><span class="nb">cd</span> <span class="s2">"</span><span class="nv">$WORK_DIR</span><span class="s2">"</span> <span class="o">&amp;&amp;</span> claude <span class="se">\</span>
        <span class="nt">--allowedTools</span> Read,Bash,Python,WebSearch,WebFetch,Write <span class="se">\</span>
        <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$STUDENT_PROMPT</span><span class="s2">"</span><span class="o">)</span>

    <span class="c"># Phase 2: Teacher reviews and writes feedback</span>
    <span class="o">(</span><span class="nb">cd</span> <span class="s2">"</span><span class="nv">$WORK_DIR</span><span class="s2">"</span> <span class="o">&amp;&amp;</span> claude <span class="se">\</span>
        <span class="nt">--allowedTools</span> Read,Bash,Python,WebSearch,WebFetch,Write <span class="se">\</span>
        <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$TEACHER_PROMPT</span><span class="s2">"</span><span class="o">)</span>
<span class="k">done</span>
</code></pre></div></div> <p>The real script adds resume support, progress tracking, verification gates, and git commits after each phase. <a href="https://github.com/ggluo/claude-by-claude">Full script on GitHub</a>.</p> <h3 id="role-design">Role design</h3> <p>The two agents are defined entirely through their prompts. No code difference — just different system instructions.</p> <p><strong>Student prompt</strong> specifies:</p> <ul> <li>Exact deliverables (LaTeX slides with named sections, Python scripts with specific figure outputs, MRI demo with from-scratch CG)</li> <li>Target audience (undergraduates with basic linear algebra)</li> <li>Format constraints (<code class="language-plaintext highlighter-rouge">\begin{align*}</code>, <code class="language-plaintext highlighter-rouge">\includegraphics</code>, <code class="language-plaintext highlighter-rouge">if __name__ == "__main__"</code>)</li> <li>In rounds 2–3: path to the Teacher’s feedback file and instruction to address every point</li> </ul> <p><strong>Teacher prompt</strong> specifies:</p> <ul> <li>All files to review (slides, illustration code, MRI demo, figures, LaTeX compilation)</li> <li>Seven review criteria (technical accuracy, pedagogical clarity, visual quality, code quality, LaTeX quality, completeness, MRI application)</li> <li>Structured output format with severity levels (Critical / Important / Minor)</li> <li>A hard requirement to use the Write tool to save <code class="language-plaintext highlighter-rouge">feedback/round-N.md</code></li> </ul> <h3 id="what-i-learned">What I learned</h3> <p><strong>1. Files are the universal interface.</strong> Two CLI invocations share no state. The only way to pass information is through files on disk. This turns out to be a feature: the feedback files create a natural audit trail, and crash recovery is trivial — just read the progress file and resume.</p> <p><strong>2. Agents sometimes talk instead of write.</strong> The Teacher occasionally outputs its review as chat text rather than calling the Write tool. The fix is a fallback: capture stdout, strip ANSI codes, save it as the feedback file. If both Write and stdout are empty, exit with an error rather than silently advancing.</p> <p><strong>3. CWD matters.</strong> Early runs had feedback files landing in the script’s working directory instead of <code class="language-plaintext highlighter-rouge">$WORK_DIR/feedback/</code>. Wrapping each <code class="language-plaintext highlighter-rouge">claude</code> call with <code class="language-plaintext highlighter-rouge">(cd "$WORK_DIR" &amp;&amp; claude ...)</code> fixed this. Absolute paths in prompts (for the feedback file path) add a second layer of safety.</p> <p><strong>4. Three rounds is about right.</strong> One round catches the big problems (undefined LaTeX commands, crashing scripts). Two rounds refines the pedagogy (wrong figure on wrong slide, missing definitions). Three rounds polishes the details (overfull boxes, CLI flags, edge-case comments). A fourth round would probably hit diminishing returns.</p> <h3 id="results">Results</h3> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 1000px)" srcset="/assets/img/blogs/agent/slide-1-1000.webp"/> <source class="responsive-img-srcset" media="(max-width: 1200px)" srcset="/assets/img/blogs/agent/slide-1-1200.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blogs/agent/slide-1-1400.webp"/> <img src="/assets/img/blogs/agent/slide-1.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="Introduction at the beginning" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> This is the first round. The illustration generated by Tikz is a bit off. </div> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 1000px)" srcset="/assets/img/blogs/agent/slide-2-1000.webp"/> <source class="responsive-img-srcset" media="(max-width: 1200px)" srcset="/assets/img/blogs/agent/slide-2-1200.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blogs/agent/slide-2-1400.webp"/> <img src="/assets/img/blogs/agent/slide-2.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="Introduction in the end" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> This is the last round. The illustration generated by Tikz is perfect. </div> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 1000px)" srcset="/assets/img/blogs/agent/figure-1-1000.webp"/> <source class="responsive-img-srcset" media="(max-width: 1200px)" srcset="/assets/img/blogs/agent/figure-1-1200.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blogs/agent/figure-1-1400.webp"/> <img src="/assets/img/blogs/agent/figure-1.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="A figure at the beginning" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> This is the first round. The MRI results figure doesn't come with clean caption and two subfigures are missing. </div> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 1000px)" srcset="/assets/img/blogs/agent/figure-2-1000.webp"/> <source class="responsive-img-srcset" media="(max-width: 1200px)" srcset="/assets/img/blogs/agent/figure-2-1200.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blogs/agent/figure-2-1400.webp"/> <img src="/assets/img/blogs/agent/figure-2.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="A figure in the end" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> This is the last round. The MRI results figure is perfect. </div> <h3 id="the-generated-materials">The generated materials</h3> <p>The final output is a self-contained lecture package:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>materials/
├── slides/
│   ├── lecture.tex          # 788-line LaTeX Beamer source
│   ├── lecture.pdf          # Compiled 26-page PDF
│   └── figures/             # 9 vector figures
├── code/
│   ├── illustrations/       # Figure-generation scripts
│   └── mri_demo/            # Multi-coil SENSE reconstruction
├── feedback/
│   ├── round-1.md           # Teacher review: 3 critical + 8 important
│   ├── round-2.md           # Teacher review: all critical resolved
│   └── round-3.md           # Teacher review: approaching lecture-ready
└── requirements.txt
</code></pre></div></div> <p>Not bad for a shell script that just calls <code class="language-plaintext highlighter-rouge">claude -p</code> in a loop.</p> <h3 id="running-your-own">Running your own</h3> <p>The script is topic-agnostic. Edit the three prompt variables and the <code class="language-plaintext highlighter-rouge">CLAUDE.md</code> to change the subject. The same loop structure works for any task that benefits from iterative review: documentation, code review, lesson plans, grant proposals.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:ggluo/claude-by-claude.git
<span class="nb">cd </span>claude-by-claude
./create-slides.sh              <span class="c"># start from scratch</span>
./create-slides.sh <span class="nt">--resume</span>     <span class="c"># resume after a crash</span>
</code></pre></div></div>]]></content><author><name></name></author><summary type="html"><![CDATA[Motivation]]></summary></entry><entry><title type="html">Optimizing softmax on GPU</title><link href="https://ggluo.github.io/blog/softmax" rel="alternate" type="text/html" title="Optimizing softmax on GPU"/><published>2025-12-29T23:19:00+00:00</published><updated>2025-12-29T23:19:00+00:00</updated><id>https://ggluo.github.io/blog/softmax</id><content type="html" xml:base="https://ggluo.github.io/blog/softmax"><![CDATA[<h3 id="introduction">Introduction</h3> <p>Softmax is a fundamental operation in deep learning, particularly in attention mechanisms of transformer models. While mathematically simple, its efficient implementation on GPU hardware presents interesting challenges and optimization opportunities. In this blog post, we’ll explore the mathematical properties of softmax, implement multiple GPU kernels with increasing sophistication, and analyze their performance characteristics. Complete code is on <a href="https://github.com/ggluo/Minimal_Softmax">GitHub Repository</a>.</p> <h3 id="softmax-translation-invariance">Softmax: Translation Invariance</h3> <p>For a vector \(\mathbf{x} = [x_1, x_2, ..., x_n]\), the softmax function is defined as:</p> \[\text{softmax}(x_i) = \frac{e^{x_i}}{\sum_{j=1}^{n} e^{x_j}}\] <p>A key property of softmax is its translation invariance:</p> \[\text{softmax}(\mathbf{x} - c) = \text{softmax}(\mathbf{x})\] <p>where \(c\) is any constant. This property is crucial for numerical stability, as we can set #$c = \max(\mathbf{x})#$ to avoid overflow in exponential calculations—this is known as “safe softmax.”</p> <p>This property has a important insight: softmax can be computed incrementally using a reduction approach. If we partition a vector into two intervals \(I_1\) and \(I_2\), we can compute:</p> <p>Let \(m_1 = \max(I_1)\), \(m_2 = \max(I_2)\), and \(m = \max(m_1, m_2)\).</p> <p>Then for the exponential sums:</p> \[l = l_1 \cdot e^{m_1 - m} + l_2 \cdot e^{m_2 - m}\] <p>where \(l_1 = \sum_{i \in I_1} e^{x_i - m_1}\) and \(l_2 = \sum_{i \in I_2} e^{x_i - m_2}\).</p> <p>This incremental computation forms the theoretical foundation for FlashAttention’s tiling strategy, which reduces HBM (High Bandwidth Memory) accesses by computing softmax in SRAM (static RAM) with minimal data movement.</p> <h3 id="implementation-journey-five-gpu-kernels">Implementation Journey: Five GPU Kernels</h3> <p>We implemented five progressively optimized softmax kernels in CUDA to demonstrate different optimization techniques. Let’s examine each approach:</p> <h4 id="kernel-0-the-baseline">Kernel 0: The Baseline</h4> <div class="language-cuda highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Simple grid-stride loop</span>
<span class="k">__global__</span> <span class="kt">void</span> <span class="nf">softmax_basic</span><span class="p">(</span><span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">input</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">output</span><span class="p">,</span> <span class="kt">int</span> <span class="n">batch_size</span><span class="p">,</span> <span class="kt">int</span> <span class="n">dim</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">row</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">row</span> <span class="o">&gt;=</span> <span class="n">batch_size</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    
    <span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">row_input</span> <span class="o">=</span> <span class="n">input</span> <span class="o">+</span> <span class="n">row</span> <span class="o">*</span> <span class="n">dim</span><span class="p">;</span>
    <span class="kt">float</span><span class="o">*</span> <span class="n">row_output</span> <span class="o">=</span> <span class="n">output</span> <span class="o">+</span> <span class="n">row</span> <span class="o">*</span> <span class="n">dim</span><span class="p">;</span>
    
    <span class="c1">// Find max</span>
    <span class="kt">float</span> <span class="n">row_max</span> <span class="o">=</span> <span class="o">-</span><span class="n">INFINITY</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">dim</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">row_max</span> <span class="o">=</span> <span class="n">fmaxf</span><span class="p">(</span><span class="n">row_max</span><span class="p">,</span> <span class="n">row_input</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span>
    <span class="p">}</span>
    
    <span class="c1">// Compute sum of exponentials</span>
    <span class="kt">float</span> <span class="n">sum_exp</span> <span class="o">=</span> <span class="mf">0.0f</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">dim</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">sum_exp</span> <span class="o">+=</span> <span class="n">expf</span><span class="p">(</span><span class="n">row_input</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">-</span> <span class="n">row_max</span><span class="p">);</span>
    <span class="p">}</span>
    
    <span class="c1">// Normalize</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">dim</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">row_output</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">expf</span><span class="p">(</span><span class="n">row_input</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">-</span> <span class="n">row_max</span><span class="p">)</span> <span class="o">/</span> <span class="n">sum_exp</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>This naive implementation suffers from multiple passes over the data and poor memory coalescing.</p> <h4 id="kernel-1-shared-memory-optimization">Kernel 1: Shared Memory Optimization</h4> <p>Kernel 1 introduces shared memory for parallel reduction within a thread block. Each block processes one row, with threads cooperatively computing the maximum and sum through shared memory reductions.</p> <p><strong>Key Insight</strong>: Shared memory is ~100x faster than global memory, making reduction operations significantly faster.</p> <h4 id="kernel-2-warp-level-primitives">Kernel 2: Warp-Level Primitives</h4> <p>Kernel 2 leverages CUDA’s warp shuffle instructions (<code class="language-plaintext highlighter-rouge">__shfl_sync</code>) for efficient warp-level reductions:</p> <div class="language-cuda highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__inline__</span> <span class="k">__device__</span> <span class="kt">float</span> <span class="nf">warpReduceMax</span><span class="p">(</span><span class="kt">float</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">offset</span> <span class="o">=</span> <span class="n">WARP_SIZE</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span> <span class="n">offset</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="n">offset</span> <span class="o">/=</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">fmaxf</span><span class="p">(</span><span class="n">val</span><span class="p">,</span> <span class="n">__shfl_down_sync</span><span class="p">(</span><span class="mh">0xffffffff</span><span class="p">,</span> <span class="n">val</span><span class="p">,</span> <span class="n">offset</span><span class="p">));</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">val</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Advantage</strong>: Warp shuffle instructions bypass shared memory entirely, using register-to-register communication within a warp.</p> <h4 id="kernel-3-2d-block-organization">Kernel 3: 2D Block Organization</h4> <p>Kernel 3 organizes threads in a 2D block (32×4), where each warp processes one row independently. This improves occupancy by allowing multiple warps per block while maintaining efficient warp-level reductions.</p> <h4 id="kernel-4-multi-row-per-warp-with-templates">Kernel 4: Multi-Row Per Warp with Templates</h4> <p>The most sophisticated kernel uses template parameters to process multiple rows per warp:</p> <div class="language-cuda highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="kt">int</span> <span class="n">ROWS_PER_WARP</span><span class="p">,</span> <span class="kt">int</span> <span class="n">COLS_PER_THREAD</span><span class="p">&gt;</span>
<span class="k">__global__</span> <span class="kt">void</span> <span class="nf">softmax_warp_multi_row</span><span class="p">(</span><span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">input</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">output</span><span class="p">,</span> 
                                       <span class="kt">int</span> <span class="n">batch_size</span><span class="p">,</span> <span class="kt">int</span> <span class="n">dim</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Each warp processes ROWS_PER_WARP rows</span>
    <span class="c1">// Each thread processes COLS_PER_THREAD elements per row</span>
    <span class="c1">// Uses register caching for maximum performance</span>
<span class="p">}</span>
</code></pre></div></div> <h4 id="performance-comparison">Performance Comparison</h4> <p>We benchmarked all kernels on an NVIDIA RTX 4500 Ada GPU with the following configuration:</p> <ul> <li>Batch size: 32768</li> <li>Dimension: 128</li> <li>Repetitions: 100</li> <li>CUDA version: 12.8</li> </ul> <table> <thead> <tr> <th>Kernel</th> <th>Description</th> <th>Avg Time (ms)</th> <th>Throughput</th> <th>Speedup vs Baseline</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>Baseline</td> <td>0.29562</td> <td>14.2M elements/ms</td> <td>1.0×</td> </tr> <tr> <td>1</td> <td>Shared Memory</td> <td>0.09587</td> <td>43.8M elements/ms</td> <td>3.1×</td> </tr> <tr> <td>2</td> <td>Warp Primitives</td> <td>0.03016</td> <td>139.1M elements/ms</td> <td>9.8×</td> </tr> <tr> <td>3</td> <td>2D Block</td> <td>0.01770</td> <td>236.9M elements/ms</td> <td>16.7×</td> </tr> <tr> <td>4</td> <td>Multi-row per Warp</td> <td>0.01669</td> <td>251.2M elements/ms</td> <td>17.7×</td> </tr> </tbody> </table> <p><br/></p> <h4 id="key-observations">Key Observations</h4> <ol> <li> <p><strong>Warp-level optimizations dominate</strong>: Kernels 2-4 show dramatic improvements by leveraging warp shuffle instructions, with Kernel 4 achieving 17.7× speedup over the baseline.</p> </li> <li> <p><strong>Memory hierarchy matters</strong>: Kernel 1’s 3.1× improvement comes primarily from using shared memory for reductions.</p> </li> <li> <p><strong>Algorithm meets architecture</strong>: Kernel 4’s template-based approach demonstrates how algorithm design must consider hardware constraints (warp size, register count).</p> </li> </ol> <h3 id="conclusion">Conclusion</h3> <p>Softmax optimization on GPU demonstrates the beautiful interplay between mathematical theory and hardware architecture, from the basic translation invariance property to sophisticated warp-level implementations. The 17.7× speedup from Kernel 0 to Kernel 4 shows how far we can push performance when we deeply understand both the algorithm and the hardware. These techniques form the foundation for more complex optimizations like FlashAttention.</p>]]></content><author><name></name></author><category term="cuda"/><category term="self-attention"/><category term="numerical computation"/><category term="coding"/><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">The details of flash attention - algorithm</title><link href="https://ggluo.github.io/blog/flash-attention-1" rel="alternate" type="text/html" title="The details of flash attention - algorithm"/><published>2025-12-17T17:09:00+00:00</published><updated>2025-12-17T17:09:00+00:00</updated><id>https://ggluo.github.io/blog/flash</id><content type="html" xml:base="https://ggluo.github.io/blog/flash-attention-1"><![CDATA[<p>The compuation of self-attention is a key component in transformer models. It is used to compute the attention weights from the query and key vectors and perform the weighted sum of the value vectors. The attention weights are computed using the dot product of the query and key vectors, and then normalized using the softmax function. This step is computationally expensive in terms of both time and memory as it faces memory bound and compute bound issues.</p> <p>We start with the definition where inputs \(Q, K, V\) typically have shapes \((B, \text{nhead}, N, nd)\). Since batch and head dimensions are independent from attention weights computation, we focus on the sequence length \(N\) and the embedding dimension \(nd\). The illustration below breaks the computation of a single head of self-attention into matrice multiplications. The attention formular is \(\begin{equation} O=\text{Attn}(Q, K, V) = \text{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V~. \end{equation}\) The k-th row of the output matrix is \(\begin{equation} O[k, :]=\sum_{i=1}^N X_s[k, i]\cdot V[i, :]~, \end{equation}\) where \(\begin{align} X_s[k, i]&amp;=\text{softmax}\{i=1:N\vert x_i=X[k,i]=\frac{Q[k, :]K[i, :]^T}{\sqrt{d_k}}\}\\ &amp;=\{i=1:N|\frac{e^{x_i}}{\sum_{j=1}^N e^{x_j}}\}~. \end{align}\)</p> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 1000px)" srcset="/assets/img/blogs/mat_attn-1000.webp"/> <source class="responsive-img-srcset" media="(max-width: 1200px)" srcset="/assets/img/blogs/mat_attn-1200.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/assets/img/blogs/mat_attn-1400.webp"/> <img src="/assets/img/blogs/mat_attn.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" title="computation of self-attention" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption_post" style="margin-bottom: 1.15rem"> </div> </div> <p>We cannot simply exponentiate the dot products because \(e^{x_i}\) can easily overflow floating-point limits. To fix this, we use safe softmax. This requires finding the maximum value in the row (\(m_N\)) and subtracting it from every element before exponentiation. Then, this computation involves three distinct steps (loops) over the data:</p> <ol> <li> <p>Find the Global Max (\(m_N\)): Iterate through the row to find \(m_N = \max(x_i)\).</p> </li> <li> <p>Compute the Global Sum (\(d_T\)): Iterate again to compute the normalization constant \(d_T = \sum_{i=1}^{N} e^{x_i - m_N}\).</p> </li> <li> <p>Compute the Output: Iterate a third time to calculate the weighted sum \(O[k, :] = \sum_{i=1}^{N} \frac{e^{x_i - m_N}}{d_T} \cdot V[i, :]\).</p> </li> </ol> <p>This approach forces us to separate the calculation into multiple passes. We cannot compute the output until we have finished scanning the entire row to find \(m_N\) and \(d_T\). This requires repeated reading and writing of the \(N \times N\) matrix makes standard attention memory-bounded.</p> <p>To optimize this, we must ask: Can we merge these main blocks?.</p> <p>We need a way to calculate the softmax incrementally (online) as we read the data, rather than waiting for the global statistics. This is achieved by maintaining running statistics that update dynamically. Instead of calculating the final max and sum upfront, we define running variables for the \(i\)-th step: 1) the running maximum: \(m_i\); 2) the running sum of exponentials: \(d'_i=\sum_{j=1}^{i} e^{x_j - m_i}\).</p> <p>Then we have two main steps. In loop 1: \(i=1 \rightarrow N\) when moving from step \(i-1\) to \(i\), we introduce a new value \(x_i\). If \(x_i &gt; m_{i-1}\), our maximum changes. We must rescale our previous accumulations to account for this change.</p> <ol> <li> <p>Updating the Max: \(m_i = \max(m_{i-1}, x_i)\)</p> </li> <li> <p>Updating the Sum (\(d'_i\)) by rescaling the previous sum (\(d'_{i-1}\)) by the exponential difference between the old and new max: \(d'_i = d'_{i-1} \cdot e^{m_{i-1} - m_i} + e^{x_i - m_i}\)</p> </li> </ol> <p>Upon reaching the N-th step, we have \(d'_T=d_T\). Then we start loop 2: \(i=1 \rightarrow N\), \(O[k, :] = \sum_{i=1}^{N} \frac{e^{x_i - m_N}}{d'_T} \cdot V[i, :]\)</p> <p>Can we merge these two loops? Yes, but the second loop is dependent on \(m_N\) and \(d'_T\). Then, we apply the same rescaling logic to the partial output accumulator by defining \(O'_i = \sum_{j=1}^i \frac{e^{x_j - m_i}}{d'_i} \cdot V[j, :]\). As a result, the computation of the output matrix \(O[k, :]\) can be recast into a geometric progression:</p> \[\begin{align} O'_i &amp;= \sum_{j=1}^i \frac{e^{x_j - m_i}}{d'_i} \cdot V[j, :] \\ &amp; = \sum_{j=1}^{i-1} \frac{e^{x_j - m_i}}{d'_i} \cdot V[j, :] + \frac{e^{x_i - m_i}}{d'_i} \cdot V[j, :] \\ &amp; = \sum_{j=1}^{i-1} \frac{e^{x_j - m_{i-1}}}{d'_{i-1}} \cdot \left( \frac{d'_{i-1}}{e^{x_j - m_{i-1}}} \cdot \frac{e^{x_j - m_i}}{d'_i} \right) \cdot V[j, :] + \frac{e^{x_i - m_i}}{d'_i} \cdot V[i, :] \\ &amp; = O'_{i-1} \cdot \left( \frac{d'_{i-1}}{d'_i} \cdot e^{m_{i-1} - m_i} \right) + \frac{e^{x_i - m_i}}{d'_i} \cdot V[i, :] \end{align}\] <p>Upon reaching the N-th step with these recursive formulas, we have \(O'_T[k, :] = O[k, :]\). By doing this, we can compute the output in a single pass without needing the computation of \(X\) matrix, and write/read it repeatedly to get global max and sum upfront. The next blog post will focus on how to implement this in an efficient way using CUDA.</p>]]></content><author><name></name></author><category term="generative models"/><category term="self-attention"/><category term="coding"/><summary type="html"><![CDATA[The compuation of self-attention is a key component in transformer models. It is used to compute the attention weights from the query and key vectors and perform the weighted sum of the value vectors. The attention weights are computed using the dot product of the query and key vectors, and then normalized using the softmax function. This step is computationally expensive in terms of both time and memory as it faces memory bound and compute bound issues.]]></summary></entry><entry><title type="html">Discrete diffusion model - 2</title><link href="https://ggluo.github.io/blog/dllm2" rel="alternate" type="text/html" title="Discrete diffusion model - 2"/><published>2025-11-08T20:38:00+00:00</published><updated>2025-11-08T20:38:00+00:00</updated><id>https://ggluo.github.io/blog/dllm2</id><content type="html" xml:base="https://ggluo.github.io/blog/dllm2"><![CDATA[<p>In this post, I’ll try to explain the core idea behind discrete diffusion models. Let’s define our setting:</p> <ul> <li><strong>Data (\(x_0\)):</strong> A sequence of tokens \(x_0 = (x_0^{(1)}, x_0^{(2)}, \dots, x_0^{(L)})\).</li> <li><strong>Vocabulary (\(V\)):</strong> Each token comes from a discrete vocabulary of size \(K\). For text, this is \(V = \{\text{"a"}, \text{"aardvark"}, \dots, \text{"zygote"}, \text{"[MASK]"}\}\).</li> <li><strong>Representation:</strong> We can represent a single token \(x^{(i)}\) as a <strong>one-hot vector</strong> of size \(K\).</li> </ul> <p>The core idea is to replace the <em>addition of Gaussian noise</em> with <em>multiplication by a transition matrix</em>.</p> <hr/> <p>The forward process \(q(x_t \vert x_{t-1})\) defines the probability of transitioning from a token at step \(t-1\) to a token at step \(t\). This is defined by a \(K \times K\) stochastic matrix \(Q_t\). The element \([Q_t]_{ij}\) represents the probability of a token \(i\) turning into a token \(j\) in one step.</p> <p>\begin{equation} q(x_t^{(i)} = j \vert x_{t-1}^{(i)} = i) = [Q_t]_{ij} \end{equation}</p> <p>If we use one-hot vectors for \(x_{t-1}\) (where it’s a row vector), this transition is a simple matrix multiplication:</p> <p>\begin{equation} q(x_t \vert x_{t-1}) = \text{Cat}(x_t ; p = x_{t-1} Q_t) \end{equation} This means the probability distribution for \(x_t\) is a Categorical distribution, with probabilities given by the vector \(x_{t-1} Q_t\). This matrix \(Q_t\) is <strong>not learned</strong>; it is a fixed schedule, just like the \(\beta_t\) schedule in continuous models. The key is in <em>how</em> we design \(Q_t\). Here are the two most common designs for text:</p> <h5 id="design-1-uniform-token-switching-diffusion">Design 1: Uniform (“Token-Switching”) Diffusion</h5> <p>This matrix says: at step \(t\), with probability \(1-\beta_t\) keep the token the same, and with probability \(\beta_t\), switch it to <em>any</em> token in the vocabulary (including itself) with uniform probability \(1/K\).</p> <p>Let \(\beta_t\) be the “noise” schedule. The matrix \(Q_t\) is: \(Q_t = (1 - \beta_t) \cdot I + \beta_t \cdot \frac{1}{K} \mathbb{1}\mathbb{1}^T\)</p> <ul> <li>\(I\) is the \(K \times K\) identity matrix.</li> <li>\(\mathbb{1}\mathbb{1}^T\) is a \(K \times K\) matrix of all ones.</li> <li>\([Q_t]_{ij} = (1 - \beta_t)\) if \(i=j\), and \(\beta_t/K\) if \(i \neq j\). (Note: the diagonal is actually \((1-\beta_t) + \beta_t/K\))</li> </ul> <h5 id="design-2-absorbing-masking-diffusion">Design 2: Absorbing (“Masking”) Diffusion</h5> <p>This is the most common for text, as it’s analogous to BERT. We add a special <code class="language-plaintext highlighter-rouge">[MASK]</code> token to the vocabulary (let’s say it’s at index \(m\)).</p> <p>This matrix says: at step \(t\), with probability \(1-\beta_t\) keep the token the same, and with probability \(\beta_t\), <strong>replace it with the <code class="language-plaintext highlighter-rouge">[MASK]</code> token</strong>. Once a token becomes <code class="language-plaintext highlighter-rouge">[MASK]</code>, it can <em>never</em> change back.</p> <p>The matrix \(Q_t\) looks like this:</p> \[\begin{equation} [Q_t]_{ij} = \left\{ \begin{array}{ll} 1 &amp; \text{if } i = j = m \text{ ([MASK] stays [MASK])} \\ 1 - \beta_t &amp; \text{if } i = j \neq m \text{ (Token stays itself)} \\ \beta_t &amp; \text{if } j = m,\, i \neq m \text{ (Token becomes [MASK])} \\ 0 &amp; \text{otherwise ([MASK] never becomes a token)} \end{array} \right. \end{equation}\] <p>This is an “absorbing state,” which is why it’s called absorbing diffusion.</p> <p>Just like in continuous models, we want to jump from \(x_0\) to \(x_t\) in one step. Thanks to the Markov property, this is just matrix multiplication:</p> <p>\begin{equation} \bar{Q}_t = Q_1 Q_2 \dots Q_t \end{equation} Then, the probability of \(x_t\) given \(x_0\) is:</p> <p>\begin{equation} q(x_t \vert x_0) = \text{Cat}(x_t ; p = x_0 \bar{Q}_t) \end{equation} This \(\bar{Q}_t\) matrix is pre-calculated and fixed. The element \([\bar{Q}_t]_{ij}\) gives the total probability that an initial token \(i\) will have become token \(j\) after \(t\) steps of noising.</p> <p>This is the “denoising” step where the neural network comes in. We need to learn a model \(p_\theta\) that approximates the “ground truth” posterior \(q(x_{t-1} \vert x_t, x_0)\).</p> <p>Using Bayes’ theorem, we can derive this ground truth posterior:</p> <p>\begin{equation} q(x_{t-1} \vert x_t, x_0) \propto q(x_t \vert x_{t-1}, x_0) \cdot q(x_{t-1} \vert x_0) \end{equation} Because the forward process is Markovian, \(q(x_t \vert x_{t-1}, x_0) = q(x_t \vert x_{t-1})\).</p> <p>\begin{equation} q(x_{t-1} \vert x_t, x_0) \propto \underbrace{q(x_t \vert x_{t-1})}_{\text{from } Q_t} \cdot \underbrace{q(x_{t-1} \vert x_0)}_{\text{from} \bar{Q}_{t-1}} \end{equation}</p> <p>This gives us a (very messy) but <strong>fully computable</strong> categorical distribution for \(x_{t-1}\). In one-hot vector notation (with \(\odot\) as element-wise multiplication):</p> <p>\begin{equation} p = \underbrace{x_t (Q_t)^T}_{\text{Prob. of } x_t \text{ from } x_{t-1}} \odot \underbrace{x_0 \bar{Q}_{t-1}}_{\text{Prob. of } x_{t-1} \text{ from } x_0} \end{equation}</p> <p>\begin{equation} q(x_{t-1} \vert x_t, x_0) = \text{Cat}\left(x_{t-1} ; p’ = \frac{p}{\sum p}\right) \end{equation}</p> <p>This formula tells us, “Given the corrupted \(x_t\) and the original \(x_0\), what is the exact probability distribution for what \(x_{t-1}\) was?”</p> <p>The model \(p_\theta(x_{t-1} \vert x_t)\) is trained to match this ground truth distribution.</p> <ol> <li><strong>Model \(\theta\):</strong> This is typically a Transformer. It takes \(x_t\) and the timestep \(t\) as input.</li> <li><strong>Prediction:</strong> The Transformer’s job is to predict \(\hat{x}_0\) (the “clean” text). <ul> <li> \[\hat{p}_0 = \text{softmax}(\text{Transformer}(x_t, t))\] </li> <li> \[\hat{x}_0 = \text{one-hot}(\text{argmax}(\hat{p}_0))\] </li> </ul> </li> <li><strong>Loss:</strong> The model \(p_\theta\) is formed by plugging this \(\hat{x}_0\) prediction into the Bayes formula from step 3.</li> </ol> <p>\begin{equation} p_\theta(x_{t-1} \vert x_t) \approx q(x_{t-1} \vert x_t, \hat{x}_0) \end{equation}</p> <p>The training loss is then a <strong>Cross-Entropy</strong> loss (or KL divergence) that forces this \(p_\theta\) distribution to match the true \(q(x_{t-1} \vert x_t, x_0)\) distribution.</p> <p>In simple terms: the Transformer (like BERT) is trained to look at a corrupted sentence (e.g., “The <code class="language-plaintext highlighter-rouge">[MASK]</code> brown fox”) and predict the original (“The quick brown fox”). This prediction is then used to calculate the “denoising” probability for the previous timestep.</p>]]></content><author><name></name></author><category term="generative models"/><category term="diffusion models"/><summary type="html"><![CDATA[In this post, I’ll try to explain the core idea behind discrete diffusion models. Let’s define our setting:]]></summary></entry><entry><title type="html">Discrete diffusion model - 1</title><link href="https://ggluo.github.io/blog/dllm" rel="alternate" type="text/html" title="Discrete diffusion model - 1"/><published>2025-11-08T20:04:00+00:00</published><updated>2025-11-08T20:04:00+00:00</updated><id>https://ggluo.github.io/blog/dllm</id><content type="html" xml:base="https://ggluo.github.io/blog/dllm"><![CDATA[<p>Discrete diffusion model is getting viral. People from image generation field are wondering if we could use accelerated sampling to generate discrete sequences. However, this is hard to achieve with simple ideas, e.g., DDIM, that we have seen in continuous diffusion models used in image generation. The fundamental challenge stems from the difference between <strong>continuous</strong> and <strong>discrete</strong> state spaces.</p> <p>The mathematical “trick” of DDIM relies entirely on the data living in a continuous vector space (like \(\mathbb{R}^n\), where images live).</p> <p>Let’s look at the deterministic DDIM equation again: \(\begin{equation} x_{t-1} = \underbrace{\sqrt{\bar{\alpha}_{t-1}} \hat{x}_0(x_t, t)}_{\text{Component 1}} + \underbrace{\sqrt{1 - \bar{\alpha}_{t-1}} \cdot \epsilon_\theta(x_t, t)}_{\text{Component 2}} \end{equation}\)</p> <p>This equation is a <strong>linear interpolation</strong> in a vector space. It works because:</p> <ol> <li><strong>Scaling:</strong> You can scale a vector (an image) by a scalar like \(\sqrt{\bar{\alpha}_{t-1}}\).</li> <li><strong>Addition:</strong> You can add two vectors (the “clean” component and the “noise” component) together.</li> </ol> <p><strong>In a discrete diffusion model (like for text), these operations are meaningless.</strong></p> <ul> <li><strong>The Data:</strong> Your data \(x_t\) is not a vector of real numbers. It’s a sequence of integers (token IDs) from a finite vocabulary \(V = \{1, 2, ..., \vert V \vert\}\).</li> <li><strong>The “Noise”:</strong> The forward process isn’t adding Gaussian noise. It’s applying a <em>probabilistic transition matrix</em> \(Q_t\). This usually means “randomly replace a token with <code class="language-plaintext highlighter-rouge">[MASK]</code>” or “randomly replace a token with another token.”</li> <li><strong>The Problem:</strong> What is \(\sqrt{0.5} \times \text{"hello"}\)? What is \(\text{"hello"} + \text{"world"}\)? These operations are undefined. You cannot perform linear interpolation on token IDs.</li> </ul> <p>Because the math of DDPM/DDIM doesn’t apply, discrete diffusion models (like D3PM or Mask-GIT) use a completely different mathematical framework.</p> <ol> <li><strong>Forward Process \(q(x_t \vert x_{t-1})\):</strong> <ul> <li>This is defined by a <strong>transition matrix</strong> \(Q_t\).</li> <li>For example, \(Q_t(j \vert i)\) might be the probability of token \(i\) at \(t-1\) becoming token \(j\) at time \(t\). (e.g., 90% chance of staying the same, 5% chance of becoming <code class="language-plaintext highlighter-rouge">[MASK]</code>, 5% chance of becoming a random token).</li> </ul> </li> <li><strong>Reverse Process \(p_\theta(x_{t-1} \vert x_t)\):</strong> <ul> <li>The model \(\theta\) is trained to <em>invert</em> this transition.</li> <li>It takes the corrupted text \(x_t\) (e.g., “The <code class="language-plaintext highlighter-rouge">[MASK]</code> brown fox”) and predicts the probability distribution for the <em>previous</em> state \(x_{t-1}\) (e.g., “The <code class="language-plaintext highlighter-rouge">[MASK]</code> brown fox”).</li> <li>Crucially, the model outputs a <strong>categorical distribution</strong> over the entire vocabulary for each token.</li> <li> \[p_\theta(x_{t-1} \vert x_t) = \text{softmax}(\dots)\] </li> </ul> </li> <li><strong>Sampling (The “Denoising” Step):</strong> <ul> <li>To get \(x_{t-1}\) from \(x_t\), you must <strong>sample</strong> from this predicted categorical distribution.</li> <li> \[x_{t-1} \sim p_\theta(\cdot \vert x_t)\] </li> <li>This step is <em>inherently stochastic</em>. You are drawing a token from a probability distribution.</li> </ul> </li> </ol> <h4 id="so-what-is-the-discrete-deterministic-equivalent">So, what is the discrete “deterministic” equivalent?</h4> <p>You can’t have the DDIM-style interpolation, but you <em>can</em> make the discrete sampling step deterministic. The closest equivalent to a “deterministic” step in a discrete model is not DDIM, but <strong>greedy decoding (i.e., <code class="language-plaintext highlighter-rouge">argmax</code>)</strong>. Instead of <em>sampling</em> from the predicted distribution \(p_\theta(\cdot \vert x_t)\), you just choose the single most likely token at each position:</p> <p>\begin{equation} x_{t-1} = \text{argmax}(p_\theta(\cdot \vert x_t)) \end{equation}</p> <p>This is deterministic, but it is not “DDIM” and doesn’t share its mathematical properties. It’s simply greedy sampling, which often leads to lower-quality and less diverse results compared to stochastic sampling. The challenge of creating a “fast sampling” method for discrete models that <em>also</em> maintains high quality (the way DDIM does for continuous models) is an active area of research. These methods are typically called “ancestral sampling” modifications, not “implicit” models.</p>]]></content><author><name></name></author><category term="generative models"/><category term="diffusion models"/><summary type="html"><![CDATA[Discrete diffusion model is getting viral. People from image generation field are wondering if we could use accelerated sampling to generate discrete sequences. However, this is hard to achieve with simple ideas, e.g., DDIM, that we have seen in continuous diffusion models used in image generation. The fundamental challenge stems from the difference between continuous and discrete state spaces.]]></summary></entry><entry><title type="html">Data consistency in Imaging</title><link href="https://ggluo.github.io/blog/data-consistency" rel="alternate" type="text/html" title="Data consistency in Imaging"/><published>2025-11-06T14:40:00+00:00</published><updated>2025-11-06T14:40:00+00:00</updated><id>https://ggluo.github.io/blog/data-consistency</id><content type="html" xml:base="https://ggluo.github.io/blog/data-consistency"><![CDATA[<p>You’ve hit on a key difference between two common approaches to data consistency. The paper uses the pseudoinverse ($A^+$), while tools like BART often use the adjoint ($A^*$), and they are used for fundamentally different reasons.</p> <ul> <li> <p><em>*$A^</em>$ (Adjoint) is for Gradient Descent:** Using the adjoint $A^<em>$ (as in $x_t \leftarrow \tilde{x}_t - \alpha A^</em>(A\tilde{x}_t - \tilde{y}_t)$) is a <strong>gradient descent step</strong>. It takes a <em>small step</em> in the direction that <em>reduces</em> the k-space error. This is common in iterative optimization algorithms that slowly converge to a solution.</p> </li> <li> <p><strong>$A^+$ (Pseudoinverse) is for Projection:</strong> The paper’s <strong>Equation (24)</strong> is not a gradient step; it’s a <strong>one-shot orthogonal projection</strong>. This operation takes the current image estimate $\tilde{x}_t$ and finds the <em>closest possible</em> image $x_t$ that is in <strong>perfect agreement</strong> with the measurements $\tilde{y}_t$.</p> </li> </ul> <h3 id="why-this-paper-uses-a">Why This Paper Uses $A^+$</h3> <p>The method described here, which the authors relate to “range-null decomposition”, relies on this direct projection. The goal of this specific step is not just to “reduce” the error, but to <strong>enforce</strong> perfect data consistency on the noiseless estimate.</p> <p>As the paper itself notes, this hard projection is only suitable for <strong>clean (noiseless) measurements</strong>. If the data $y_0$ were noisy, this step would perfectly inject all that noise into the final image. That is why they use a different “Plug-and-Play (PnP)” method (Algorithm 2) for the noisy data scenario.</p> <p>This formula is the direct solution to a constrained optimization problem. Here is the derivation.</p> <h3 id="1--the-optimization-problem">1. 🎯 The Optimization Problem</h3> <p>The goal is to <strong>enforce data consistency</strong>. We have a current image estimate, $\tilde{x}_t$, which is good but doesn’t perfectly match our measurements, $\tilde{y}_t$.</p> <p>We want to find a new image, $x_t$, that solves the following problem:</p> <blockquote> <p>“Find the image $x_t$ that is <strong>as close as possible</strong> to $\tilde{x}_t$ while <strong>perfectly satisfying</strong> the measurement constraint $A(x_t) = \tilde{y}_t$.”</p> </blockquote> <p>Mathematically, this is written as:</p> <p>\(\text{minimize} \quad \frac{1}{2} ||x - \tilde{x}_t||_2^2\) \(\text{subject to} \quad A(x) = \tilde{y}_t\)</p> <table> <tbody> <tr> <td>(We use $\frac{1}{2}</td> <td> </td> <td>\cdot</td> <td> </td> <td>_2^2$ because it has the same minimum as $</td> <td> </td> <td>\cdot</td> <td> </td> <td>_2$ and its derivative is simpler.)</td> </tr> </tbody> </table> <hr/> <h3 id="2--the-method-lagrange-multipliers">2. 📝 The Method: Lagrange Multipliers</h3> <p>We can solve this constrained problem by introducing a Lagrange multiplier, $\lambda$, and forming the Lagrangian function, $\mathcal{L}$:</p> \[\mathcal{L}(x, \lambda) = \underbrace{\frac{1}{2} ||x - \tilde{x}_t||_2^2}_{\text{Proximity to } \tilde{x}_t} + \underbrace{\lambda^T (A(x) - \tilde{y}_t)}_{\text{Constraint}}\] <p>To find the minimum, we must find the point where the gradients of $\mathcal{L}$ with respect to both $x$ and $\lambda$ are zero.</p> <hr/> <h3 id="3-️-solving-the-system">3. ⚙️ Solving the System</h3> <h4 id="step-1-find-the-gradient-with-respect-to-x">Step 1: Find the gradient with respect to $x$</h4> <p>We take the partial derivative of $\mathcal{L}$ with respect to $x$ and set it to zero.</p> <blockquote> <table> <tbody> <tr> <td>(Note: The derivative of $\frac{1}{2}</td> <td> </td> <td>x - c</td> <td> </td> <td>_2^2$ with respect to $x$ is $(x - c)$. The derivative of $\lambda^T A(x)$ with respect to $x$ is $A^T\lambda$.)</td> </tr> </tbody> </table> </blockquote> \[\frac{\partial \mathcal{L}}{\partial x} = (x - \tilde{x}_t) + A^T\lambda = 0\] <p>Solving for $x$, we get: \(x = \tilde{x}_t - A^T\lambda\) This tells us the <em>form</em> of our solution $x_t$, but we still need to find $\lambda$.</p> <h4 id="step-2-use-the-constraint-to-find-lambda">Step 2: Use the constraint to find $\lambda$</h4> <p>We know our solution $x_t$ <em>must</em> satisfy the constraint $A(x_t) = \tilde{y}_t$. So, we plug our formula for $x$ from Step 1 into the constraint:</p> \[A(\tilde{x}_t - A^T\lambda) = \tilde{y}_t\] <p>Now we distribute $A$: \(A(\tilde{x}_t) - A(A^T\lambda) = \tilde{y}_t\)</p> <p>And rearrange to solve for $\lambda$: \(A(\tilde{x}_t) - \tilde{y}_t = A A^T \lambda\)</p> <p>\(\lambda = (A A^T)^+ (A(\tilde{x}_t) - \tilde{y}_t)\) (We use the pseudoinverse $(A A^T)^+$ in case $A A^T$ is not invertible).</p> <h4 id="step-3-put-it-all-together">Step 3: Put it all together</h4> <p>Now we substitute this expression for $\lambda$ back into our solution for $x$ from Step 1:</p> \[x_t = \tilde{x}_t - A^T \left[ (A A^T)^+ (A(\tilde{x}_t) - \tilde{y}_t) \right]\] <p>This looks complicated, but we can simplify it using a standard identity of the Moore-Penrose pseudoinverse:</p> <blockquote> <p><strong>Pseudoinverse Identity:</strong> $A^T(A A^T)^+ = A^+$</p> </blockquote> <p>Applying this identity, the $A^T (A A^T)^+$ term simplifies to just $A^+$:</p> \[x_t = \tilde{x}_t - A^+ (A(\tilde{x}_t) - \tilde{y}_t)\] <p>This is <strong>Equation (24)</strong>. It is the exact, analytical solution to the problem of finding the closest image to $\tilde{x}_t$ that perfectly matches the measurements $\tilde{y}_t$.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[You’ve hit on a key difference between two common approaches to data consistency. The paper uses the pseudoinverse ($A^+$), while tools like BART often use the adjoint ($A^*$), and they are used for fundamentally different reasons.]]></summary></entry><entry><title type="html">Pseudoinverse Derivation</title><link href="https://ggluo.github.io/blog/pseudoinverse" rel="alternate" type="text/html" title="Pseudoinverse Derivation"/><published>2025-11-06T13:53:00+00:00</published><updated>2025-11-06T13:53:00+00:00</updated><id>https://ggluo.github.io/blog/pseudoinverse</id><content type="html" xml:base="https://ggluo.github.io/blog/pseudoinverse"><![CDATA[<p>The concept of a pseudoinverse arises because we often encounter linear systems \(Ax = b\) that don’t have a unique, exact solution. The least-squares framework provides a way to find the “best” possible solution. This derivation is typically split into two main cases, both of which fall under the umbrella of “least-squares.”</p> <hr/> <h4 id="1-the-overdetermined-case-tall-matrix-m--n">1. The overdetermined case (tall matrix, \(m &gt; n\))</h4> <p>This is the classic least-squares problem. We have more equations (\(m\)) than unknowns (\(n\)), and the system is likely inconsistent (meaning no \(x\) perfectly satisfies \(Ax = b\)).</p> <p><strong>The Goal:</strong> Find the vector \(x\) that minimizes the squared error, or the L2-norm of the residual vector \(r = Ax - b\). \begin{equation} x^* = \arg \min_{x} |Ax - b|^2 \end{equation}</p> <p>Let’s define the squared error \(E(x)\) as a function of \(x\). Using the dot product, the squared norm is \(r^T r\): \begin{equation} E(x) = (Ax - b)^T (Ax - b) \end{equation} We expand the terms:</p> \[\begin{aligned} E(x)&amp;= (x^T A^T - b^T) (Ax - b) \\ E(x)&amp;= x^T A^T A x - x^T A^T b - b^T A x + b^T b \end{aligned}\] <p>Notice that \(x^T A^T b\) and \(b^T A x\) are both scalars. In fact, they are transposes of each other, and since the transpose of a scalar is just the scalar itself, they are equal: \((b^T A x)^T = x^T A^T b\). So, we can combine them: \begin{align} E(x) = x^T A^T A x - 2 x^T A^T b + b^T b \end{align}</p> <p>To find the \(x\) that minimizes this error, we take the gradient of \(E(x)\) with respect to \(x\) and set it to zero. Using the matrix calculus rules:</p> <ul> <li>\(\nabla_x (x^T B x) = (B + B^T) x\) (which is \(2Bx\) if \(B\) is symmetric)</li> <li>\(\nabla_x (c^T x) = c\) (or \(\nabla_x (x^T c) = c\))</li> </ul> <p>The matrix \(A^T A\) is symmetric. Applying these rules, we get:</p> <p>\begin{equation} \nabla_x E(x) = 2(A^T A) x - 2(A^T b) + 0 \end{equation}</p> <p>Set the gradient to zero to find the minimum: \(\begin{align} 2(A^T A) x - 2(A^T b) =&amp; 0 \\ (A^T A) x =&amp; A^T b \end{align}\)</p> <p>This final equation, \(A^T A x = A^T b\), is known as the <strong>normal equation</strong>. To solve for \(x\), we can multiply by the inverse of \((A^T A)\) and get \(x = (A^T A)^{-1} A^T b\). This step requires a critical assumption: <strong>\((A^T A)\) must be invertible</strong>. This is true if and only if \(A\) has <strong>linearly independent columns</strong> (i.e., \(A\) has full column rank).</p> <p>We are looking for a matrix \(A^+\) (the pseudoinverse) such that \(x = A^+ b\). By direct comparison, we find the <strong>left pseudoinverse</strong>:</p> <p>\begin{equation} A^+ = (A^T A)^{-1} A^T \end{equation}</p> <p>It’s called the “left” inverse because if you multiply \(A\) on the left by \(A^+\), you get the identity matrix: \(A^+ A = \left[ (A^T A)^{-1} A^T \right] A = (A^T A)^{-1} (A^T A) = I\).</p> <hr/> <h4 id="2-the-underdetermined-case-wide-matrix-n--m">2. The underdetermined case (wide matrix, \(n &gt; m\))</h4> <p>This is a different kind of “least-squares” problem. We have fewer equations (\(m\)) than unknowns (\(n\)), so the system $Ax = b$ has infinitely many solutions.</p> <p><strong>The Goal:</strong> Of all the possible solutions, find the <em>unique</em> solution $x$ that has the <strong>minimum norm</strong> (\(\min \|x\|^2\)). This is the “least-squares” solution in the sense that it’s the “smallest” solution vector. We want to solve the constrained optimization problem:</p> <p>\begin{equation} \min |x|^2 \quad \text{subject to} \quad Ax = b \end{equation}</p> <p>We can solve this using the method of Lagrange multipliers. The Lagrangian function \(L\) is:</p> <p>\begin{equation} L(x, \lambda) = x^T x + \lambda^T (Ax - b) \end{equation}</p> <p>Here, \(x^T x\) is \(\|x\|^2\) and \(\lambda\) is a vector of Lagrange multipliers. We find the minimum by setting the gradients with respect to both \(x\) and \(\lambda\) to zero.</p> <p><strong>Gradient w.r.t. \(x\):</strong></p> \[\begin{aligned} \nabla_x L &amp;= 2x + A^T \lambda = 0\\ x &amp;= -\frac{1}{2} A^T \lambda \end{aligned}\] <p><strong>Gradient w.r.t. \(\lambda\):</strong></p> \[\begin{aligned} \nabla_\lambda L &amp;= Ax - b = 0\\ Ax &amp;= b \end{aligned}\] <p>Now we combine these two results. Substitute the expression for \(x\) into the constraint equation:</p> \[\begin{aligned} A \left( -\frac{1}{2} A^T \lambda \right) &amp;= b\\ -\frac{1}{2} (A A^T) \lambda &amp;= b \end{aligned}\] <p>Now, solve for the multiplier vector: \(\lambda = -2 (A A^T)^{-1} b\)</p> <p>This step requires the assumption that <strong>\((A A^T)\) is invertible</strong>, which is true if and only if $A$ has <strong>linearly independent rows</strong> (i.e., \(A\) has full row rank).</p> <hr/> <p>The full <strong>Moore-Penrose pseudoinverse</strong> (often derived using Singular Value Decomposition, or SVD) generalizes both of these cases. It also works for matrices that are <em>rank-deficient</em> (i.e., neither full column nor full row rank), which is when \((A^T A)\) or \((A A^T)\) would be singular (non-invertible).</p> <p>However, the “least-squares” perspective naturally gives rise to these two fundamental forms:</p> <table> <thead> <tr> <th style="text-align: left">Case</th> <th style="text-align: left">Matrix Shape</th> <th style="text-align: left">Problem</th> <th style="text-align: left">Solution \(x = A^+ b\)</th> <th style="text-align: left">Pseudoinverse \(A^+\)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Overdetermined</strong></td> <td style="text-align: left">Tall (\(m &gt; n\)), full column rank</td> <td style="text-align: left">\(\min |Ax - b|^2\)</td> <td style="text-align: left">Least-Squares Solution</td> <td style="text-align: left">\(A^+ = (A^T A)^{-1} A^T\)</td> </tr> <tr> <td style="text-align: left"><strong>Underdetermined</strong></td> <td style="text-align: left">Wide (\(n &gt; m\)), full row rank</td> <td style="text-align: left">\(\min |x|^2\) s.t. \(Ax=b\)</td> <td style="text-align: left">Minimum Norm Solution</td> <td style="text-align: left">\(A^+ = A^T (A A^T)^{-1}\)</td> </tr> </tbody> </table> <hr/> <h4 id="3-pseudoinverse-using-singular-value-decomposition">3. Pseudoinverse using singular value decomposition</h4> <p>Here is the derivation of the pseudoinverse using the Singular Value Decomposition (SVD). This is the most general and powerful approach, as it works for <strong>all</strong> matrices, including those that are rank-deficient (where the previous least-squares derivations would fail).</p> <p>First, any \(m \times n\) matrix \(A\) can be decomposed using SVD as:</p> <p>\begin{equation} A = U \Sigma V^T \end{equation}</p> <p>Where:</p> <ul> <li><strong>\(U\)</strong>: An \(m \times m\) orthogonal matrix (\(U^T U = I_m\)). Its columns are the <em>left singular vectors</em>.</li> <li><strong>\(V\)</strong>: An \(n \times n\) orthogonal matrix (\(V^T V = I_n\)). Its columns are the <em>right singular vectors</em>.</li> <li><strong>\(\Sigma\)</strong>: An \(m \times n\) diagonal matrix containing the singular values \(\sigma_1, \sigma_2, ...\) in decreasing order. These values are non-negative.</li> </ul> <p>If \(A\) has rank \(r\), then \(r\) of the singular values are positive, and the rest are zero.</p> <p>The key to the SVD approach is defining the pseudoinverse of the simple diagonal matrix \(\Sigma\).</p> <p>Let’s say \(\Sigma\) (an \(m \times n\) matrix) looks like this:</p> \[\Sigma = \begin{bmatrix} \sigma_1 &amp; 0 &amp; \dots &amp; 0 \\ 0 &amp; \sigma_2 &amp; \dots &amp; 0 \\ \vdots &amp; \vdots &amp; \ddots &amp; \vdots \\ 0 &amp; 0 &amp; \dots &amp; \sigma_n \\ \vdots &amp; \vdots &amp; \dots &amp; \vdots \\ 0 &amp; 0 &amp; \dots &amp; 0 \end{bmatrix}\] <p>To find its pseudoinverse \(\Sigma^+\), we do two things:</p> <ol> <li><strong>Transpose</strong> its dimensions to get an \(n \times m\) matrix.</li> <li><strong>Reciprocate</strong> all the <strong>non-zero</strong> singular values, leaving the zeros as zeros.</li> </ol> <p>If \(\Sigma_{ii} = \sigma_i &gt; 0\), then \((\Sigma^+)_{ii} = 1/\sigma_i\). If \(\Sigma_{ii} = 0\), then \((\Sigma^+)_{ii} = 0\).</p> <p><strong>Example:</strong></p> <p>If</p> \[\Sigma = \begin{bmatrix} \sigma_1 &amp; 0 &amp; 0 \\ 0 &amp; \sigma_2 &amp; 0 \\ 0 &amp; 0 &amp; 0 \\ 0 &amp; 0 &amp; 0 \end{bmatrix}\] <p>then</p> \[\Sigma^+ = \begin{bmatrix} 1/\sigma_1 &amp; 0 &amp; 0 &amp; 0 \\ 0 &amp; 1/\sigma_2 &amp; 0 &amp; 0 \\ 0 &amp; 0 &amp; 0 &amp; 0 \end{bmatrix}\] <p>With \(\Sigma^+\) defined, the pseudoinverse of \(A\) (called the <strong>Moore-Penrose Pseudoinverse</strong>) is simply:</p> <p>\begin{equation} A^+ = V \Sigma^+ U^T \end{equation}</p> <p>This definition elegantly handles all cases: overdetermined, underdetermined, and rank-deficient. Now, let’s show that this SVD definition is consistent with the two cases we derived earlier.</p> <h5 id="case-1-overdetermined-tall-matrix-m--n-full-column-rank-rn">Case 1: overdetermined (tall matrix, \(m &gt; n\), full column rank \(r=n\))</h5> <ul> <li>Our previous formula was \(A^+ = (A^T A)^{-1} A^T\).</li> <li>Let’s plug the SVD into this formula and see if we get \(V \Sigma^+ U^T\).</li> </ul> <ol> <li> <p><strong>\(A^T A\)</strong>: \(A^T A = (U \Sigma V^T)^T (U \Sigma V^T) = (V \Sigma^T U^T) (U \Sigma V^T)\) Since \(U^T U = I\), this simplifies to: \(A^T A = V (\Sigma^T \Sigma) V^T\)</p> </li> <li> <p><strong>\((A^T A)^{-1}\)</strong>: \((A^T A)^{-1} = (V (\Sigma^T \Sigma) V^T)^{-1}\) Using the rule \((XYZ)^{-1} = Z^{-1} Y^{-1} X^{-1}\) and knowing \(V^{-1} = V^T\): \((A^T A)^{-1} = (V^T)^{-1} (\Sigma^T \Sigma)^{-1} V^{-1} = V (\Sigma^T \Sigma)^{-1} V^T\)</p> </li> <li> <p><strong>\(A^+ = (A^T A)^{-1} A^T\)</strong>: \(A^+ = \left[ V (\Sigma^T \Sigma)^{-1} V^T \right] (U \Sigma V^T)^T\) \(A^+ = \left[ V (\Sigma^T \Sigma)^{-1} V^T \right] (V \Sigma^T U^T)\) Since \(V^T V = I\), this simplifies to: \(A^+ = V (\Sigma^T \Sigma)^{-1} \Sigma^T U^T\)</p> </li> <li>analyze the \(\Sigma\) part: <ul> <li>\(\Sigma\) is \(m \times n\). Let’s say \(\Sigma = \begin{bmatrix} D_n \\ 0 \end{bmatrix}\), where \(D_n\) is an \(n \times n\) diagonal matrix of \(\sigma_i\).</li> <li>\(\Sigma^T = \begin{bmatrix} D_n &amp; 0 \end{bmatrix}\).</li> <li>\(\Sigma^T \Sigma = \begin{bmatrix} D_n &amp; 0 \end{bmatrix} \begin{bmatrix} D_n \\ 0 \end{bmatrix} = D_n^2\). (This is an \(n \times n\) invertible matrix).</li> <li>\((\Sigma^T \Sigma)^{-1} = (D_n^2)^{-1} = D_n^{-2}\).</li> <li>Now, \((\Sigma^T \Sigma)^{-1} \Sigma^T = D_n^{-2} \begin{bmatrix} D_n &amp; 0 \end{bmatrix} = \begin{bmatrix} D_n^{-1} &amp; 0 \end{bmatrix}\).</li> <li>This resulting matrix, \(\begin{bmatrix} D_n^{-1} &amp; 0 \end{bmatrix}\), is <strong>exactly the SVD definition of \(\Sigma^+\)</strong>.</li> </ul> </li> <li>Conclusion: \(A^+ = V (\Sigma^+) U^T\). The formulas match.</li> </ol> <h5 id="case-2-underdetermined-wide-matrix-n--m-full-row-rank-rm">Case 2: underdetermined (wide matrix, \(n &gt; m\), full row rank \(r=m\))</h5> <ul> <li>Our previous formula was \(A^+ = A^T (A A^T)^{-1}\).</li> <li> <p>A similar substitution (which I’ll skip the full algebra for) shows: \(A^+ = (V \Sigma^T U^T) \left[ (U \Sigma V^T)(V \Sigma^T U^T) \right]^{-1}\) \(A^+ = (V \Sigma^T U^T) \left[ U (\Sigma \Sigma^T) U^T \right]^{-1}\) \(A^+ = (V \Sigma^T U^T) \left[ U (\Sigma \Sigma^T)^{-1} U^T \right]\) \(A^+ = V \Sigma^T (\Sigma \Sigma^T)^{-1} U^T\)</p> </li> <li>Again, if you analyze the \(\Sigma\) part (\(\Sigma^T (\Sigma \Sigma^T)^{-1}\)), you’ll find it simplifies to \(\Sigma^+\).</li> <li><strong>Conclusion</strong>: \(A^+ = V \Sigma^+ U^T\). The formulas match again.</li> </ul> <h5 id="why-the-svd-method-is-better">Why the SVD Method is Better</h5> <p>The SVD derivation \(A^+ = V \Sigma^+ U^T\) is superior because it <strong>never fails</strong>.</p> <ul> <li>In the rank-deficient case, \(r &lt; \min(m, n)\).</li> <li>This means both \((A^T A)\) and \((A A^T)\) will be singular (non-invertible) because their \(\Sigma\) components (\(D_r^2\)) will contain zeros. Our first derivation using \((...)^{-1}\) would fail.</li> <li>However, the SVD definition of \(\Sigma^+\) <strong>gracefully handles this</strong>. It only reciprocates the \(\sigma_i\) that are <em>non-zero</em> and leaves the zeros as zeros.</li> </ul> <p>This \(A^+ = V \Sigma^+ U^T\) always gives the unique <strong>minimum-norm least-squares solution</strong>:</p> <ol> <li><strong>Least-Squares</strong>: It finds the \(x\) (or \(x\)’s) that minimizes the error \(\|Ax - b\|^2\).</li> <li><strong>Minimum-Norm</strong>: If there are multiple solutions (as in the underdetermined or rank-deficient case), it picks the <em>one and only one</em> solution \(x\) that also has the smallest length \(\|x\|^2\).</li> </ol>]]></content><author><name></name></author><category term="linear algebra"/><category term="coding"/><summary type="html"><![CDATA[The concept of a pseudoinverse arises because we often encounter linear systems \(Ax = b\) that don’t have a unique, exact solution. The least-squares framework provides a way to find the “best” possible solution. This derivation is typically split into two main cases, both of which fall under the umbrella of “least-squares.”]]></summary></entry><entry><title type="html">How Bindings Work with I/O in TensorRT</title><link href="https://ggluo.github.io/blog/tensorrt-bindings" rel="alternate" type="text/html" title="How Bindings Work with I/O in TensorRT"/><published>2023-03-08T11:31:00+00:00</published><updated>2023-03-08T11:31:00+00:00</updated><id>https://ggluo.github.io/blog/bindings-in-tensorrt</id><content type="html" xml:base="https://ggluo.github.io/blog/tensorrt-bindings"><![CDATA[<p>In TensorRT, a “binding” refers to the association between a tensor in the computational graph (network) and a specific memory buffer that holds the data for that tensor during inference. Bindings are used to specify how input and output tensors of a neural network are mapped to memory buffers where the input data is provided and where the output data is retrieved after inference.</p> <p>Here’s a basic overview of how bindings work in TensorRT from the docs from <a href="https://docs.nvidia.com/deeplearning/tensorrt/api/python_api/infer/Core/Engine.html?highlight=binding#tensorrt.ICudaEngine">TensorRT engine</a> :</p> <ol> <li> <p><strong>Input Bindings</strong>: Input bindings specify where the input data for the neural network will be located during inference. Typically, this involves specifying memory buffers where the input data is stored, such as CPU or GPU memory.</p> </li> <li> <p><strong>Output Bindings</strong>: Output bindings specify where the output data produced by the neural network during inference will be placed. Similar to input bindings, output bindings specify memory buffers where the output data will be stored, such as CPU or GPU memory.</p> </li> <li> <p><strong>Binding Indices</strong>: Each input and output tensor in the network is associated with a unique binding index. These binding indices are used to identify which input or output tensor corresponds to which memory buffer during inference.</p> </li> <li> <p><strong>Binding Shapes</strong>: Along with specifying the memory location for input and output tensors, bindings also specify the shape (dimensions) of each tensor. This ensures that the memory buffers provided for input and output data have the correct size to accommodate the tensors produced by the network.</p> </li> <li> <p><strong>Binding Data Types</strong>: Bindings also specify the data type of each tensor, such as float32, int8, etc. This ensures that the memory buffers provided for input and output data have the correct data type to match the tensors produced by the network.</p> </li> </ol>]]></content><author><name></name></author><category term="c++"/><category term="tensorRT"/><summary type="html"><![CDATA[In TensorRT, a “binding” refers to the association between a tensor in the computational graph (network) and a specific memory buffer that holds the data for that tensor during inference. Bindings are used to specify how input and output tensors of a neural network are mapped to memory buffers where the input data is provided and where the output data is retrieved after inference.]]></summary></entry><entry><title type="html">How to Call C++ Class from C</title><link href="https://ggluo.github.io/blog/call-cpp-class-from-c" rel="alternate" type="text/html" title="How to Call C++ Class from C"/><published>2023-01-27T09:54:00+00:00</published><updated>2023-01-27T09:54:00+00:00</updated><id>https://ggluo.github.io/blog/how-to-call-cpp-class-from-c</id><content type="html" xml:base="https://ggluo.github.io/blog/call-cpp-class-from-c"><![CDATA[<p>Calling a C++ class from C can be a bit tricky because C does not support classes and objects like C++. However, you can still interact with C++ code from C by creating a C interface for your C++ class. In this example, we define a C++ class MyClass with a method doSomething(). Then, we create a C wrapper MyClassWrapper that provides functions to create, manipulate, and destroy instances of MyClass. Finally, we can use these functions in a C program to interact with the MyClass object. See <a href="https://github.com/ggluo/Call-cpp-class-from-c">github</a>.</p> <h2 id="define-a-c-class">Define a C++ class</h2> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#ifndef MYCLASS_H
#define MYCLASS_H
</span>
<span class="k">class</span> <span class="nc">MyClass</span> <span class="p">{</span>
<span class="nl">public:</span>
    <span class="n">MyClass</span><span class="p">();</span>
    <span class="kt">void</span> <span class="n">doSomething</span><span class="p">();</span>
<span class="nl">private:</span>
    <span class="kt">int</span> <span class="n">data</span><span class="p">;</span>
<span class="p">};</span>
<span class="cp">#endif // MYCLASS_H
</span></code></pre></div></div> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"MyClass.h"</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="n">MyClass</span><span class="o">::</span><span class="n">MyClass</span><span class="p">()</span> <span class="o">:</span> <span class="n">data</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="p">{}</span>

<span class="kt">void</span> <span class="n">MyClass</span><span class="o">::</span><span class="n">doSomething</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Doing something in MyClass"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="define-a-c-wrapper">Define a C wrapper</h2> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// MyClassWrapper.h</span>
<span class="cp">#ifndef MYCLASSWRAPPER_H
#define MYCLASSWRAPPER_H
</span>
<span class="cp">#ifdef __cplusplus
</span><span class="k">extern</span> <span class="s">"C"</span> <span class="p">{</span>
<span class="cp">#endif
</span>
<span class="k">typedef</span> <span class="kt">void</span><span class="o">*</span> <span class="n">MyClassHandle</span><span class="p">;</span>

<span class="n">MyClassHandle</span> <span class="n">createMyClass</span><span class="p">();</span>
<span class="kt">void</span> <span class="n">destroyMyClass</span><span class="p">(</span><span class="n">MyClassHandle</span> <span class="n">obj</span><span class="p">);</span>
<span class="kt">void</span> <span class="n">doSomethingInMyClass</span><span class="p">(</span><span class="n">MyClassHandle</span> <span class="n">obj</span><span class="p">);</span>

<span class="cp">#ifdef __cplusplus
</span><span class="p">}</span>
<span class="cp">#endif
</span>
<span class="cp">#endif // MYCLASSWRAPPER_H
</span>
</code></pre></div></div> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"MyClassWrapper.h"</span><span class="cp">
#include</span> <span class="cpf">"MyClass.h"</span><span class="cp">
</span>
<span class="k">extern</span> <span class="s">"C"</span> <span class="p">{</span>

<span class="n">MyClassHandle</span> <span class="n">createMyClass</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">return</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">MyClassHandle</span><span class="o">&gt;</span><span class="p">(</span><span class="k">new</span> <span class="n">MyClass</span><span class="p">());</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">destroyMyClass</span><span class="p">(</span><span class="n">MyClassHandle</span> <span class="n">obj</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">delete</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">MyClass</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">obj</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">doSomethingInMyClass</span><span class="p">(</span><span class="n">MyClassHandle</span> <span class="n">obj</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">MyClass</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">obj</span><span class="p">)</span><span class="o">-&gt;</span><span class="n">doSomething</span><span class="p">();</span>
<span class="p">}</span>
<span class="p">}</span>

</code></pre></div></div> <h2 id="use-the-wrapper-in-c">Use the wrapper in C</h2> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"MyClassWrapper.h"</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">MyClassHandle</span> <span class="n">obj</span> <span class="o">=</span> <span class="n">createMyClass</span><span class="p">();</span>
    <span class="n">doSomethingInMyClass</span><span class="p">(</span><span class="n">obj</span><span class="p">);</span>
    <span class="n">destroyMyClass</span><span class="p">(</span><span class="n">obj</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div>]]></content><author><name></name></author><category term="c++"/><summary type="html"><![CDATA[Calling a C++ class from C can be a bit tricky because C does not support classes and objects like C++. However, you can still interact with C++ code from C by creating a C interface for your C++ class. In this example, we define a C++ class MyClass with a method doSomething(). Then, we create a C wrapper MyClassWrapper that provides functions to create, manipulate, and destroy instances of MyClass. Finally, we can use these functions in a C program to interact with the MyClass object. See github.]]></summary></entry></feed>